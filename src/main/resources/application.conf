akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "DEBUG"
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"
}

microservice2 {
  listen-address = "0.0.0.0"
  listen-port = 8080
  database = {
    connectionPool = "HikariCP"
    numThreads = 20
    queueSize = 1000
    url = "jdbc:postgresql://192.168.10.157/microservice"
    user = "microservice"
    password = "microservice"
  }
}
